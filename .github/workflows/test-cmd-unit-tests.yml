name: Test Build Runner (parallel & sequential)

on:
  push:
    paths:
      - "cmd/cmd/build.go"
      - "cmd/cmd/build_parallel_test.go"
      - ".github/workflows/test-build-runner.yml"
      - "pkg/build/**"
  pull_request:
    paths:
      - "cmd/cmd/build.go"
      - "cmd/cmd/build_parallel_test.go"
      - ".github/workflows/test-build-runner.yml"
      - "pkg/build/**"
  workflow_dispatch:

jobs:
  test:
    name: Run cmd unit tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run cmd tests
        run: go test ./cmd/cmd/...
